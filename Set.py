"""
Sets are used to store multiple items in a single variable.
Set items are unordered, unchangeable, does not allow duplicate values and hold different data types.
Set items are indexed which starts with 0.
Sets are denoted with {}
"""

thisset = {"apple", 2, "cherry", "apple"}
print(thisset)
print(len(thisset))